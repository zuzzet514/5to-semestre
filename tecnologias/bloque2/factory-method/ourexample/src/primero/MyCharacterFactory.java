package primero;
/*
import implement1.Warrior;
import implement1.Wizard;

public class MyCharacterFactory extends CharacterFactory {
    // si el usuario no escoje entre Wizard y Orc,
    // se selecciona Warrior por default
    @Override
    public Character createCharacter(int selection) {
        switch (selection) {
            case 1:
                return new Wizard();
            case 2:
                return new Orc();
            default:
                return new Warrior();
        }
    }
}

 */
